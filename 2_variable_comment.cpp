#include <iostream>
using namespace std;
// What's a low level program? The program which as near to binary(bare metal) is known as low level.
// what's a high level program? The program which is far to binary(bare metal) is known as high level.

int main()
{
    // variales are container to store data. Like container of water(bottles), clothes(bag), pen(geometry box)
    int a = 1;
    int b = 2;
    int c = a + b;
    // These a, b, c are variable of type int means it's a container of int data type.
    // the thing I am writing won't become part of your program. It's just for reminding an passing relevent context data to fellow humans and known as comments.
    // it's ignored.
    cout << c << endl;
    /*
    By the way it's multi line comment
    Just to write multiple line comment
    instead of doing `//` again and again
    */
    return 0;
}
